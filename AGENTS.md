# AGENTS.md

## 工程设计原则

1. **先确认领域不变量，再简化实现。**
   只有明确哪些条件固定成立，才能安全删除分支、参数和中间状态，避免建立在错误假设上的简化。

2. **分离数据形状与实际尺度。**
   数据源只描述归一化形状，尺寸、幅度等缩放策略由使用方负责，使同一份数据不依赖具体配置。

3. **固定条件不要表现为配置。**
   永远不变的值应成为代码中的明确不变量，而不是继续传递、存储和验证的可配置字段。

4. **可推导值不要重复配置。**
   分辨率、步长、数量等派生信息应由必要输入计算，避免多个事实来源逐渐产生不一致。

5. **架构边界按职责和变化原因划分。**
   读写、算法和运行时消费应各自负责完整职责，并通过清晰的数据结构交换结果，而不是按文件长度机械拆分。

6. **函数返回语义结果，不泄漏内部中间量。**
   调用方应获得真正需要的结果；只服务于内部公式的余弦、比例和临时状态应封装在实现中。

7. **不要为了缩短函数而拆函数。**
   只有具有独立概念、输入和输出的子流程才值得提取；单纯透传或包装会割裂执行流程。

8. **优先寻找数学等价的化简。**
   先消除重复计算、互逆运算和可直接推导的临时量，再考虑缓存、并行或更复杂的性能优化。

9. **数值稳定性优先于盲目提高精度。**
   应优先选择避免大数相减、小角度精度丢失等问题的稳定公式，而不是只把数据类型升级为更高精度。

10. **精度由最终可观察结果决定。**
    根据最终存储类型、显示分辨率和实际误差影响选择精度；不会改变可见结果的额外精度通常没有价值。

11. **防御检查集中在信任边界。**
    外部输入、配置入口和持久化读取需要验证；内部已由入口保证的不变量不应在每层重复检查。

12. **保守计算应与实际后果匹配。**
    只有误差会造成可观察错误时才增加向外取整或额外余量，避免为了理论完美扩大计算链条。

13. **缓存必须具有明确的复用价值。**
    只服务一次计算、没有跨调用复用的缓存会增加生命周期和一致性成本，应优先使用直接计算。

14. **持久化格式只保存真正可变化的信息。**
    固定不变量无需写入文件；字段布局或语义变化时应更新格式版本，避免产生含义不明确的兼容行为。

15. **重构按理解、确认、实施和适度验证推进。**
    每次只修改已确认的关注点，以与风险相称的方式验证结果，不顺带扩张到未授权的相邻问题。
